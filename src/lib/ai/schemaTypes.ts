/**
 * Tipos de schema usados em responseSchema do Gemini.
 * Extraído do App.tsx (Fase 1 da refatoração).
 */

export enum Type {
  STRING = 'STRING',
  NUMBER = 'NUMBER',
  INTEGER = 'INTEGER',
  BOOLEAN = 'BOOLEAN',
  ARRAY = 'ARRAY',
  OBJECT = 'OBJECT',
}
